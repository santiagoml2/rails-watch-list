<%= render 'header' %>

<div class="container">
  <div class="movie-content">
    <div class="row">
      <div class="col-lg-6">

      </div>
      <div class="col-lg-6">
        <h2><%= @list.name %> Movies</h2>
        <div class="btn-style">
          <p><%= link_to "Bookmark", new_list_bookmark_path(@list), class: "btn-hook" %></p>
          <p><%= link_to "Edit", edit_list_path(@list), class: 'btn-edit' %></p>
          <p><%= link_to "Delete", edit_list_path(@list), class: 'btn-delete' %></p>
        </div>
        <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Inventore nam suscipit porro quasi sit, earum harum dolorem? Quaerat quidem veritatis enim suscipit minima perspiciatis nostrum sequi voluptas omnis? Repudiandae, sapiente.</p>
      </div>
    </div>

    <hr>

    <div class="movies-grid">
      <% @list.movies.each do |movie| %>
      <div class="box-poster">
        <img class= "show-movie-img" src="<%= movie.poster_url %>">
        <p class="movie-path"><%= link_to movie.title, movie_path(movie) %></p>
      </div>
      <% end %>
    </div>
  </div>
</div>
